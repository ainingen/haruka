# CLAUDE.md

『ハルカのセッティングノート』— カーチューニング × レースゲーム（ブラウザ / HTML5、PLiCy 公開想定）。

作業を始める前に `README.md` と `docs/` に目を通すこと。このファイルは、その要約ではなく
**判断に迷ったときの拘束条件**を書いたもの。

## 応答

- **このリポジトリでのやり取りはすべて日本語で行う。** コメント、コミットメッセージ、ドキュメントも日本語。
- 変数名・関数名は英語で構わないが、UI 文言とドキュメントは日本語。

## 絶対に守る制約

### 1. 実写風の車両画像を使わない

- 車体は**図面様式の線画 SVG** のみ。写真、3D レンダリング、実写風イラストは一切使わない。
- レース画面は **コース＝線（SVG `<path>` 一本）、車＝点**。自車は赤、他車は青。
- 線色は `currentColor` で、親の `color` から自車／AI 車を切り替える（`assets/svg/README.md` 参照）。
- 車体シルエットは4種（ハッチバック / セダン / GT / フォーミュラ）、**ノーズが左・リアウイングが右**で統一、同一縮尺。
- 「絵で戦わない」は妥協の産物ではなく設計の核。見栄えのために破らない。

### 2. すべてのパーツに副作用を持たせる

- **単純上位のパーツを作らない。** 上位パーツは必ず何かを失う。
- パーツは常に4点セットで定義する — **説明 / 効果 / 副作用 / 解禁段階**。
  副作用が思いつかないパーツは、まだ設計が終わっていない。
- 副作用は現実の理屈に基づくこと（出力↑なら発熱と耐久↓、ダウンフォース↑ならドラッグ↑、
  ソフトタイヤはグリップ↑だが摩耗↑、軽量化は全域有利だが剛性↓でリタイア率↑）。
- 最適解はコースとレース形式で変わるように作る。プレイヤーを**攻略ではなく理解**に向かわせる。
- 説明文は「父・大河のセッティングノートの書き込み」の文体で書く。クラス5帯のみハルカ自身の筆致に切り替わる。

### 3. 実在の名称を使わない

- **実在のレースシリーズ名・ブランド名・メーカー名・車種名・サーキット名は使用しない**（商標）。
- クラス名、スポンサー名、パーツメーカー名はすべて架空。
- 一般的な技術用語（LSD、車高調、キャンバー、ダウンフォース等）は問題ない。
- ドライバーライセンス制度など**現実の制度**は準拠してよい（`docs/設計/クラス構成.md` 参照）。
- 「ST相当」「GT下位相当」のような比較表現はドキュメント内の説明にとどめ、**ゲーム内文言には出さない**。
- ノートを「ハルノート」と呼ばせない（1941年の外交文書と重なる）。略すなら「セッティングノート」。詳細は `docs/シナリオ/キャラクター.md` の呼称ルール。

### 4. 素の HTML5 + SVG + JS のみ

- **フレームワーク・ビルドツール禁止**（React、Vue、Svelte、webpack、Vite 等）。PLiCy 公開の制約。
- npm 依存を追加しない。CDN から外部ライブラリを読み込まない。`package.json` は `"type": "module"` を宣言するためだけにあり、`dependencies` は空のまま。
- ブラウザがそのまま開ける静的ファイル構成を保つ。`index.html` を開けば動く状態が正。
- ES Modules（`<script type="module">`）は使ってよい。物理演算ライブラリは使わない
  — 走行は進行距離の毎フレーム加算方式（`docs/設計/レース方式.md`）。

## ディレクトリ

| パス | 内容 |
|---|---|
| `assets/svg/` | 車体シルエット線画（4クラス分） |
| `data/` | パーツ・コース・シナリオのデータ |
| `docs/設計/` | クラス構成、レース方式、画面設計 |
| `docs/チューニング/` | パーツ設計（6カテゴリ） |
| `docs/シナリオ/` | プロローグ台本、キャラクター |
| `src/engine/` | レース計算エンジン（純関数、I/O なし）とそのテスト。`race.js`（走行）、`economy.js`（お金・消耗）、`season.js`（シーズン・昇降格・スポンサー）、`rivals.js`（AI車）、`save.js`（進行のシリアライズ）。`npm test` |
| `src/ui/` | 画面。`season.html`（シーズンのハブ）、`setup.html`（セッティング／地は紙）、`race.html`（レース／地は画面の層）。色は `theme.css` の変数のみ。進行は URL の `s=` 1つで持ち回る（`src/engine/save.js`）。**HTTP 経由で開くこと**（`file://` では JSON と SVG と CSS を読めない） |
| `tools/` | **開発用スクリプト。配布物に含めない。** データを生成するだけで、ゲームからは読み込まない（`generate-courses.mjs` ＝ コース形状の生成） |

## 実装するときの指針

- **データとロジックを分ける。** パーツ定義・コース定義は `data/*.json` に置き、コードに数値を埋め込まない。
- UI は**紙と画面の二層**（`docs/設計/画面設計.md`）。紙＝図面・ノート・ハルカの一言（クリーム地）、
  画面＝チームのPC（黒地・等幅・緑／白／琥珀）。紙の上に画面が開く。色は CSS 変数（`--paper-*` / `--mon-*`）でしか書かない。
  点滅や数字回転などの演出は `cue(name)` を通す（後で音を同期するため）。
- セッティング画面の地は**整備マニュアル／設計図面の様式**（二重枠、寸法線、引き出し線、DETAIL枠）。
- **未装着の枠を残して「未装着」と表示する。** 空欄が成長の予告になる。消さない。
- 車高を下げたらシルエットの屋根が実際に下がる、といった**操作と表示の連動**を必ず入れる。
- レース画面には**倍速ボタン（1倍 / 4倍 / スキップ）を必ず付ける**。
- コースはセクター（ストレート / 高速コーナー / 低速コーナー）に分割し、セクターごとに効くパラメータを変える。

## キャラクターを書くとき

- ハルカ：一人称は「ウチ」。口は悪いが根は優しい。優しさは台詞で説明せず**行動で見せる**。
  主人公への呼び方は「アンタ」（幼少期は「おいちゃん」）。
- 大河：答えを与えず選ばせる。断定を避け、「このコースの、この日の正解」の語り口。
- 主人公：名前はプレイヤー入力（台本では「主人公」と表記）。大河の1つ下の後輩で、大河には敬語。
- 詳細と代表的な台詞は `docs/シナリオ/キャラクター.md` に従うこと。勝手に設定を足さない。

## 未確定事項（勝手に決めない）

- ハルカと7年間会えなかった理由
- パーツの具体的な数値
- 経済の具体値（賞金・参加費・修理費。`data/economy.json` は仮置きで、通しで走らせて直す）

決まったこと：1シーズン6戦、降格あり（クラス1からは下がらない）。`docs/設計/経済とシーズン.md`。

これらが必要になったら、実装で仮置きせず**確認する**。仮置きする場合は理由を明示し、
`README.md` の「未確定」に残す。
